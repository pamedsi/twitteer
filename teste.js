const objeto = {
    nome: 'Everaldo',
    idade: 710,
    corno: true
}

for (const key in objeto) {

    const key = {objeto}
    // if (Object.hasOwnProperty.call(objeto, key)) {
    //     const element = objeto[key];
        
    // }

    console.log(key)
}